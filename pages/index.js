
import React from "react";

export default function Home() {
  return (
    // eslint-disable-next-line react/no-unescaped-entities
    <h1> Next. js is not very flexible when it comes to routing. The default approach is page-based, where you specify whether to generate those pages server-side, client-side or static. While this is good for simple applications, if you need something more complex, you'll need to write a lot more code and make use of Node </h1>
  )
}
